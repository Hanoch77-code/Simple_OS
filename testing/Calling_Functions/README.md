# Calling Functions
- Calling functions is similar to calling function in high level language 
- But instead of specifying the function name we specify label which points to address of the function in the memory.
- input are predefined like we store the data in register already we know when passing to the function
- Like for example, we need to send a value to the next function to operate on then we send data a common editable register like bx in both calling and called function.
- Further explanation is made in the file 'boot.asm' .
- This prints a output XHello using function.
